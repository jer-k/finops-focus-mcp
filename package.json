{
  "name": "finops-focus-mcp",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "format": "biome format --write .",
    "generate": "tsx scripts/generate-focus-docs.ts",
    "lint:fix": "biome lint --write .",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "^0.5.0",
    "effect": "4.0.0-beta.10",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@types/node": "^25.3.0",
    "tsx": "^4.19.4",
    "typescript": "5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.67.0"
  }
}
